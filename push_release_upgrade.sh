#!/usr/bin/env bash
set -euo pipefail

echo "This app is currently local-first and does not have a release flow yet."
echo "Create the GitHub repo and release path first if you want push-release-upgrade behavior."
exit 1
